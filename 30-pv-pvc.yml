apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-already-created-disk-1
spec:
  capacity:
    storage: 1Gi # Specify the exact size of your EBS volume
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  awsElasticBlockStore:
    volumeID: vol-0444c3a5311acd806
    fsType: ext4